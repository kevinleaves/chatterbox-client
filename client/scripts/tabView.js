// create the template for a tab div in here

let TabView = {
  // render: _.template(`
  //     <div class="tab"> <%= tab %> </div>
  // `),
  render: _.template(`<div class="tab"> <%= tab %> </div>`),
};